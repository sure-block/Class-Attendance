<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ClasslandAttendance.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClasslandAttendance"
    xmlns:views="using:ClasslandAttendance.Views"
    Title="签到系统">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Grid x:ConnectionId='2' Grid.Row="0" x:Name="TitleBarGrid"
              Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 返回按钮 -->
            <Button x:ConnectionId='5' Grid.Column="0" x:Name="BackButton"
                    Width="48" Height="48"
                    Background="Transparent"
                    BorderBrush="Transparent"
                                            
                    ToolTipService.ToolTip="返回"
                    Visibility="Collapsed">
                <FontIcon Glyph="&#xE72B;" FontSize="16"/>
            </Button>

            <!-- 应用标题 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                <TextBlock Text="&#xE9D5;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                           VerticalAlignment="Center" Margin="0,0,10,0"
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <TextBlock Text="签到系统" FontSize="14" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- 主内容区 -->
        <Grid Grid.Row="1">
            <Frame x:ConnectionId='3' x:Name="ContentFrame"/>

            <!-- 左下角设置按钮 -->
            <Button x:ConnectionId='4' x:Name="SettingsButton"
                    Width="48" Height="48"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="12"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                
                    ToolTipService.ToolTip="设置">
                <FontIcon Glyph="&#xE713;" FontSize="16"/>
            </Button>
        </Grid>
    </Grid>
</Window>

